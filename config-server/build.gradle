moduleEnabled(!whatToTest('SCS'))

dependencies {
    compile "org.springframework.cloud:spring-cloud-config-server"
}

ext {
  fabric8 = true
  springboot = true
  BOOT_VERSION = '1.5.1.RELEASE'
}
//createDockerTaskWithPort(8888)
